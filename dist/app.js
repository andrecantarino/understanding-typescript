"use strict";
const username = 'Max';
console.log(username);
const button = document.querySelector("button");
button === null || button === void 0 ? void 0 : button.addEventListener("button", () => {
    console.log("clicked");
});
